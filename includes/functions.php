<?php

/**
 * @file Functions file for migration.
 */

/**
 * Fake function to allow the Drupal DB Abstraction layer to work.
 */
function t($string, array $args = array(), array $options = array()) {
  return $string;
}
